# Compiled binaries
gitlab-procs-exporter
gitlab-procs-exporter.exe
*.exe
*.exe~
*.dll
*.so
*.dylib
gitlab-procs-exporter-linux-*
# Root-anchored so it does NOT match the cmd/jobreport/ source directory.
/jobreport
/jobreport-linux-*
/jobreport-web
# Stray binary if `go build` is run inside the cmd/jobreport source dir.
cmd/jobreport/jobreport

# Local build output dir (make build / make build-jobreport)
.bin/

# Testing & Coverage
*.test
*.out
*.prof
coverage.txt
profile.out

# Dependency management (kept go.sum but ignore vendor/ if vendor mode is not preferred)
# vendor/

# OS specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Common Editor and IDE caches
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
.project
.classpath
.settings/

# Local logs and temporary files
*.log
# but keep the jobreport test fixtures (real job logs used by the parser tests)
!cmd/jobreport/testdata/*.log
tmp/
dist/

# ralphex progress logs
.ralphex/progress/
